---
import Base from '../layouts/Base.astro';
import Breadcrumb from '../components/Breadcrumb.astro';
import gcData from '../data/gamechangers.json';
import playerData from '../data/players.json';

const s3Matches = (gcData.seasons['3']?.matches as any[] || []).filter((m: any) => m.homeScore > 0 || m.awayScore > 0);
const s3Regular = s3Matches.filter((m: any) => m.gameweek <= 11);

const sf1Match = s3Matches.find((m: any) => m.homeSlug === 'ndl-fc' && m.awaySlug === 'sds-fc' && m.gameweek === 12);
const sf2Match = s3Matches.find((m: any) => m.homeSlug === 'deportrio' && m.awaySlug === 'prime-fc' && m.gameweek === 12);
const finalMatch = s3Matches.find((m: any) => m.homeSlug === 'ndl-fc' && m.awaySlug === 'prime-fc' && m.gameweek === 12);

const teams = [
  { name: 'NDL FC', slug: 'ndl-fc', pos: 1, pts: 26, manager: 'Niko Omilana', emoji: '🏆', color: '#00e676' },
  { name: 'Deportrio', slug: 'deportrio', pos: 2, pts: 23, manager: 'Micah Richards', emoji: '🔵', color: '#2196f3' },
  { name: 'Prime FC', slug: 'prime-fc', pos: 3, pts: 21, manager: 'KSI', emoji: '🔴', color: '#f44336' },
  { name: 'SDS FC', slug: 'sds-fc', pos: 4, pts: 20, manager: 'Tobi Brown', emoji: '🟢', color: '#4caf50' },
];

function getTopScorers(slug: string) {
  return playerData.players
    .filter((p: any) => p.teamSlug === slug && (p.seasons?.['3']?.goals || 0) > 0)
    .sort((a: any, b: any) => (b.seasons?.['3']?.goals || 0) - (a.seasons?.['3']?.goals || 0))
    .slice(0, 3)
    .map((p: any) => ({ name: p.name, goals: p.seasons['3'].goals, slug: p.slug }));
}

function getTeamStats(slug: string, matches: any[]) {
  const ms = matches.filter((m: any) => m.homeSlug === slug || m.awaySlug === slug);
  let w = 0, d = 0, l = 0, gf = 0, ga = 0;
  ms.forEach((m: any) => {
    const isHome = m.homeSlug === slug;
    gf += isHome ? m.homeScore : m.awayScore;
    ga += isHome ? m.awayScore : m.homeScore;
    if (isHome ? m.homeScore > m.awayScore : m.awayScore > m.homeScore) w++;
    else if (m.homeScore === m.awayScore) d++;
    else l++;
  });
  return { played: ms.length, w, d, l, gf, ga, gd: gf - ga };
}

function getH2H(s1: string, s2: string) {
  return s3Matches
    .filter((m: any) => (m.homeSlug === s1 && m.awaySlug === s2) || (m.homeSlug === s2 && m.awaySlug === s1))
    .map((m: any) => ({
      gw: m.gameweek,
      home: m.homeTeam,
      away: m.awayTeam,
      hs: m.homeScore,
      as: m.awayScore,
    }))
    .sort((a: any, b: any) => a.gw - b.gw);
}

const semi1 = { home: teams[0], away: teams[3], h2h: getH2H(teams[0].slug, teams[3].slug) };
const semi2 = { home: teams[1], away: teams[2], h2h: getH2H(teams[1].slug, teams[2].slug) };

teams.forEach(t => {
  (t as any).stats = getTeamStats(t.slug, s3Regular);
  (t as any).topScorers = getTopScorers(t.slug);
});

const schema = {
  "@context": "https://schema.org",
  "@type": "SportsEvent",
  "name": "Baller League UK Season 3 Final Four",
  "description": "The Baller League UK Season 3 Final Four at The O2 London on Monday 25 May 2026. NDL FC, Deportrio, Prime FC, and SDS FC battle for the championship.",
  "startDate": "2026-05-25T18:00:00+01:00",
  "location": { "@type": "Place", "name": "The O2 Arena", "address": { "@type": "PostalAddress", "addressLocality": "London", "addressCountry": "GB" } },
  "organizer": { "@type": "Organization", "name": "Baller League UK", "url": "https://ballerleague.uk" },
  "performer": [
    { "@type": "SportsTeam", "name": "NDL FC", "url": "https://ballerleagueukhub.com/teams/ndl-fc" },
    { "@type": "SportsTeam", "name": "Deportrio", "url": "https://ballerleagueukhub.com/teams/deportrio" },
    { "@type": "SportsTeam", "name": "Prime FC", "url": "https://ballerleagueukhub.com/teams/prime-fc" },
    { "@type": "SportsTeam", "name": "SDS FC", "url": "https://ballerleagueukhub.com/teams/sds-fc" }
  ]
};
---

<Base
  title="Baller League UK Final Four 2026 — Teams, Matchups, H2H & Preview"
  description="The Baller League UK Season 3 Final Four at The O2 London on Monday 25 May 2026. NDL FC vs SDS FC and Deportrio vs Prime FC in the semi-finals. Full team profiles, H2H history, key players, and format guide."
  schema={schema}
  keywords="Baller League Final Four, The O2, NDL FC, Deportrio, Prime FC, SDS FC, semi-final, playoffs, champion, 25 May 2026"
>
  <div class="page-header ff-header">
    <div class="container">
      <Breadcrumb items={[{name:'Home',url:'/'},{name:'Final Four'}]} />
      <div class="ff-badge-pulse">
        <span class="ff-badge-dot"></span>
        <span class="ff-badge-text">Season 3 Playoffs</span>
      </div>
      <h1 class="ff-main-title">The Final Four</h1>
      <p class="section-subtitle ff-subtitle">
        Monday 25 May 2026 — The O2, London — Live on Sky Sports, YouTube & Twitch
      </p>
      <a href="https://www.theo2.co.uk/events/detail/baller-league-uk-2026-final-four" target="_blank" rel="noopener noreferrer" class="btn btn-gold btn-lg ff-ticket-btn">Secure Your Tickets</a>
    </div>
  </div>

  <div class="container ff-page">

    <!-- THE QUALIFIED TEAMS -->
    <section class="ff-section">
      <div class="ff-section-header">
        <h2 class="ff-section-title">The Qualified Teams</h2>
        <p class="text-muted">The top 4 teams that battled through 11 gameweeks to reach the grand finale.</p>
      </div>
      
      <div class="ff-teams-grid">
        {teams.map(t => (
          <a href={`/teams/${t.slug}`} class={`ff-team-card card rank-${t.pos}`}>
            <div class="ff-team-rank">#{t.pos}</div>
            <div class="card-body ff-team-body">
              
              <div class="ff-team-header">
                <div class="ff-logo-container">
                  <img src={`/logos/${t.slug}.svg`} alt={t.name} class="ff-team-logo" loading="lazy" onerror="this.style.display='none';this.nextElementSibling.style.display='flex'" />
                  <span class="ff-team-logo-fallback">{t.emoji}</span>
                </div>
                <div class="ff-team-info">
                  <h3 class="ff-team-name">{t.name}</h3>
                  <div class="ff-manager-tag">
                    <img src={`/managers/${t.slug}.webp`} alt={t.manager} class="ff-manager-avatar" loading="lazy" onerror="this.hidden=true" />
                    <span>{t.manager}</span>
                  </div>
                </div>
                <div class="ff-points-box">
                  <span class="ff-points-val">{t.pts}</span>
                  <span class="ff-points-lbl">PTS</span>
                </div>
              </div>

              <div class="ff-stats-row">
                <div class="ff-stat-pill"><span class="val">{t.stats.played}</span> <span class="lbl">GP</span></div>
                <div class="ff-stat-pill"><span class="val">{t.stats.w}</span> <span class="lbl">W</span></div>
                <div class="ff-stat-pill"><span class="val">{t.stats.d}</span> <span class="lbl">D</span></div>
                <div class="ff-stat-pill"><span class="val">{t.stats.l}</span> <span class="lbl">L</span></div>
                <div class="ff-stat-pill"><span class="val">{t.stats.gf}</span> <span class="lbl">GF</span></div>
                <div class="ff-stat-pill"><span class="val">{t.stats.gd > 0 ? '+' + t.stats.gd : t.stats.gd}</span> <span class="lbl">GD</span></div>
              </div>

              <div class="ff-scorers-box">
                <div class="ff-scorers-title">Top Scorers</div>
                <div class="ff-scorers-list">
                  {t.topScorers.map(ts => (
                    <div class="ff-scorer-item">
                      <span class="name" set:html={ts.name}></span>
                      <span class="goals">{ts.goals}</span>
                    </div>
                  ))}
                </div>
              </div>

            </div>
          </a>
        ))}
      </div>
    </section>

    <!-- SEMI-FINALS -->
    <section class="ff-section">
      <div class="ff-section-header">
        <h2 class="ff-section-title">Semi-Final Matchups</h2>
        <p class="text-muted">Single elimination. Winner advances to the Final to be crowned Champion.</p>
      </div>

      <div class="ff-matchups-container">

        <!-- Semi-Final 1 -->
        <div class="ff-matchup-card card result-card">
          <div class="ff-matchup-badge">Semi-Final 1</div>
          <a href="/match/s3-ndl-fc-vs-sds-fc-gw12" class="ff-matchup-result-link">
            <div class="ff-matchup-battle">
              <div class="ff-battle-team home">
                <div class="ff-battle-info">
                  <span class="ff-seed">#1 Seed</span>
                  <h3 class="ff-battle-name">NDL FC</h3>
                  <span class="ff-battle-manager">Niko Omilana</span>
                </div>
                <div class="ff-battle-logo-wrap">
                  <img src="/logos/ndl-fc.svg" alt="NDL FC" class="ff-battle-logo" onerror="this.style.display='none';this.nextElementSibling.style.display='flex'" />
                  <span class="ff-battle-fallback">🏆</span>
                </div>
              </div>
              <div class="ff-battle-score">
                <span class="score-home win">{sf1Match.homeScore}</span>
                <span class="score-sep">–</span>
                <span class="score-away">{sf1Match.awayScore}</span>
              </div>
              <div class="ff-battle-team away">
                <div class="ff-battle-logo-wrap">
                  <img src="/logos/sds-fc.svg" alt="SDS FC" class="ff-battle-logo" onerror="this.style.display='none';this.nextElementSibling.style.display='flex'" />
                  <span class="ff-battle-fallback">🟢</span>
                </div>
                <div class="ff-battle-info">
                  <span class="ff-seed">#4 Seed</span>
                  <h3 class="ff-battle-name">SDS FC</h3>
                  <span class="ff-battle-manager">Tobi Brown</span>
                </div>
              </div>
            </div>
          </a>
          <div class="ff-h2h-section">
            <div class="ff-h2h-header">
              <h4>Season 3 Head-to-Head</h4>
              <a href="/h2h?t1=ndl-fc&t2=sds-fc" class="ff-h2h-link">Full Comparison →</a>
            </div>
            {semi1.h2h.length > 0 ? (
              <div class="ff-h2h-table">
                {semi1.h2h.map(m => (
                  <a href={`/news/ndl-fc-vs-sds-fc-gw${m.gw}`} class="ff-h2h-row">
                    <span class="gw">GW{m.gw}</span>
                    <span class="match">{m.home} <strong>{m.hs}–{m.as}</strong> {m.away}</span>
                    <span class="result">{m.hs > m.as ? `${m.home} won` : m.as > m.hs ? `${m.away} won` : 'Draw'}</span>
                  </a>
                ))}
              </div>
            ) : (
              <div class="ff-h2h-empty">No previous meetings this season.</div>
            )}
          </div>
          <a href="/news/ndl-fc-vs-sds-fc-gw12" class="ff-report-link">Read Match Report →</a>
        </div>

        <!-- Semi-Final 2 -->
        <div class="ff-matchup-card card result-card">
          <div class="ff-matchup-badge">Semi-Final 2</div>
          <a href="/match/s3-deportrio-vs-prime-fc-gw12" class="ff-matchup-result-link">
            <div class="ff-matchup-battle">
              <div class="ff-battle-team home">
                <div class="ff-battle-info">
                  <span class="ff-seed">#2 Seed</span>
                  <h3 class="ff-battle-name">Deportrio</h3>
                  <span class="ff-battle-manager">Micah Richards</span>
                </div>
                <div class="ff-battle-logo-wrap">
                  <img src="/logos/deportrio.svg" alt="Deportrio" class="ff-battle-logo" onerror="this.style.display='none';this.nextElementSibling.style.display='flex'" />
                  <span class="ff-battle-fallback">🔴</span>
                </div>
              </div>
              <div class="ff-battle-score">
                <span class="score-home">{sf2Match.homeScore}</span>
                <span class="score-sep">–</span>
                <span class="score-away win">{sf2Match.awayScore}</span>
              </div>
              <div class="ff-battle-team away">
                <div class="ff-battle-logo-wrap">
                  <img src="/logos/prime-fc.svg" alt="Prime FC" class="ff-battle-logo" onerror="this.style.display='none';this.nextElementSibling.style.display='flex'" />
                  <span class="ff-battle-fallback">⚡</span>
                </div>
                <div class="ff-battle-info">
                  <span class="ff-seed">#3 Seed</span>
                  <h3 class="ff-battle-name">Prime FC</h3>
                  <span class="ff-battle-manager">KSI</span>
                </div>
              </div>
            </div>
          </a>
          <div class="ff-h2h-section">
            <div class="ff-h2h-header">
              <h4>Season 3 Head-to-Head</h4>
              <a href="/h2h?t1=deportrio&t2=prime-fc" class="ff-h2h-link">Full Comparison →</a>
            </div>
            {semi2.h2h.length > 0 ? (
              <div class="ff-h2h-table">
                {semi2.h2h.map(m => (
                  <a href={`/news/deportrio-vs-prime-fc-gw${m.gw}`} class="ff-h2h-row">
                    <span class="gw">GW{m.gw}</span>
                    <span class="match">{m.home} <strong>{m.hs}–{m.as}</strong> {m.away}</span>
                    <span class="result">{m.hs > m.as ? `${m.home} won` : m.as > m.hs ? `${m.away} won` : 'Draw'}</span>
                  </a>
                ))}
              </div>
            ) : (
              <div class="ff-h2h-empty">No previous meetings this season.</div>
            )}
          </div>
          <a href="/news/deportrio-vs-prime-fc-gw12" class="ff-report-link">Read Match Report →</a>
        </div>

      </div>
    </section>

    <!-- THE FINAL -->
    <section class="ff-section">
      <div class="ff-section-header">
        <h2 class="ff-section-title gold-text ff-gold-glow">The Grand Final</h2>
        <p class="text-muted">Prime FC defeat NDL FC 5–2 to become Season 3 Champions.</p>
      </div>
      <div class="ff-final-container">
        <div class="ff-final-card card">
          <div class="ff-final-badge">Championship Final</div>
          <div class="ff-final-battle">
            <div class="ff-final-team">
              <div class="ff-final-logo-wrap">
                <img src="/logos/ndl-fc.svg" alt="NDL FC" class="ff-final-logo" onerror="this.style.display='none';this.nextElementSibling.style.display='flex'" />
                <span class="ff-final-fallback">🏆</span>
              </div>
              <h3 class="ff-final-name">NDL FC</h3>
              <span class="ff-final-manager">Niko Omilana</span>
              <span class="ff-final-seed">#1 Seed</span>
            </div>
            <div class="ff-final-score">
              <span class="ff-final-score-val">{finalMatch.homeScore}</span>
              <span class="ff-final-score-sep">–</span>
              <span class="ff-final-score-val win">{finalMatch.awayScore}</span>
            </div>
            <div class="ff-final-team win">
              <div class="ff-final-logo-wrap">
                <img src="/logos/prime-fc.svg" alt="Prime FC" class="ff-final-logo" onerror="this.style.display='none';this.nextElementSibling.style.display='flex'" />
                <span class="ff-final-fallback">⚡</span>
              </div>
              <h3 class="ff-final-name">Prime FC</h3>
              <span class="ff-final-manager">KSI</span>
              <span class="ff-final-seed">#3 Seed</span>
            </div>
          </div>
          <div class="ff-final-champion">
            <div class="ff-trophy-wrap">🏆</div>
            <div class="ff-champion-text">
              <span class="ff-champion-label">Season 3 Champions</span>
              <span class="ff-champion-team">Prime FC</span>
            </div>
          </div>
          <div class="ff-final-links">
            <a href="/match/s3-ndl-fc-vs-prime-fc-gw12" class="btn btn-ghost">Match Details</a>
            <a href="/news/ndl-fc-vs-prime-fc-gw12" class="btn btn-gold">Read Full Match Report</a>
          </div>
        </div>
      </div>
    </section>

    <!-- WHAT IS THE FINAL FOUR -->
    <section class="ff-section card ff-format-section">
      <div class="card-body">
        <h2 class="ff-section-title">Tournament Format</h2>
        <p class="text-secondary" style="max-width: 800px; margin-bottom: var(--space-xl);">
          After 11 gameweeks of intense regular-season action, only the <strong>top 4 teams</strong> qualify. The format is a high-stakes, single-elimination knockout tournament all taking place on one massive night.
        </p>
        <div class="ff-bracket-grid">
          <div class="ff-bracket-item">
            <div class="step">1</div>
            <h4>Semi-Final 1</h4>
            <p>1st Place vs 4th Place</p>
          </div>
          <div class="ff-bracket-item">
            <div class="step">2</div>
            <h4>Semi-Final 2</h4>
            <p>2nd Place vs 3rd Place</p>
          </div>
          <div class="ff-bracket-item final">
            <div class="step">3</div>
            <h4>The Final</h4>
            <p>Winner SF1 vs Winner SF2</p>
          </div>
        </div>
      </div>
    </section>

    <!-- PAST WINNERS -->
    <section class="ff-section">
      <h2 class="ff-section-title">Hall of Fame</h2>
      <div class="ff-past-grid">
        <div class="ff-past-card">
          <div class="season-badge">Season 1</div>
          <div class="trophy-icon">🏆</div>
          <h3 class="champion">SDS FC</h3>
          <p class="details">Manager: Tobi Brown<br/>Beat MVPs United</p>
        </div>
        <div class="ff-past-card">
          <div class="season-badge">Season 2</div>
          <div class="trophy-icon">🏆</div>
          <h3 class="champion">Wembley Rangers</h3>
          <p class="details">Manager: Ian Wright<br/>Beat SDS FC</p>
        </div>
        <div class="ff-past-card current">
          <div class="season-badge gold-text">Season 3</div>
          <div class="trophy-icon glow">🏆</div>
          <h3 class="champion gold-text">Prime FC</h3>
          <p class="details">Manager: KSI<br/>Beat NDL FC 5–2 in the Final</p>
        </div>
      </div>
    </section>

    <!-- WHERE TO WATCH -->
    <section class="ff-section">
      <h2 class="ff-section-title text-center">Where to Watch</h2>
      <div class="ff-watch-grid">
        <a href="https://www.youtube.com/@BallerLeagueUK" target="_blank" rel="noopener noreferrer" class="ff-watch-item youtube">
          <span class="icon">▶</span>
          <strong>YouTube</strong>
          <span>@BallerLeagueUK</span>
        </a>
        <a href="https://www.twitch.tv/ballerleagueuk" target="_blank" rel="noopener noreferrer" class="ff-watch-item twitch">
          <span class="icon">💬</span>
          <strong>Twitch</strong>
          <span>ballerleagueuk</span>
        </a>
        <div class="ff-watch-item sky">
          <span class="icon">📺</span>
          <strong>Sky Sports</strong>
          <span>Live Broadcast</span>
        </div>
        <a href="https://www.theo2.co.uk/events/detail/baller-league-uk-2026-final-four" target="_blank" rel="noopener noreferrer" class="ff-watch-item live">
          <span class="icon">🎟️</span>
          <strong>The O2 Arena</strong>
          <span>Get Tickets</span>
        </a>
      </div>
    </section>

  </div>
</Base>

<style>
/* Header & Hero */
.ff-header {
  background: radial-gradient(circle at 50% 100%, rgba(245, 158, 11, 0.15) 0%, var(--bg-primary) 70%), 
              linear-gradient(180deg, #111 0%, var(--bg-primary) 100%);
  padding-bottom: var(--space-3xl);
  border-bottom: 1px solid rgba(245, 158, 11, 0.3);
  text-align: center;
  position: relative;
  overflow: hidden;
}

.ff-header::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background-image: linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px),
                    linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px);
  background-size: 40px 40px;
  pointer-events: none;
}

.ff-badge-pulse {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(245, 158, 11, 0.1);
  border: 1px solid rgba(245, 158, 11, 0.3);
  padding: 6px 16px;
  border-radius: var(--radius-full);
  margin-bottom: var(--space-lg);
  position: relative;
  z-index: 1;
}

.ff-badge-dot {
  width: 8px; height: 8px;
  background: var(--gold);
  border-radius: 50%;
  box-shadow: 0 0 10px var(--gold);
  animation: pulseGold 2s infinite;
}

@keyframes pulseGold {
  0% { box-shadow: 0 0 0 0 rgba(245, 158, 11, 0.7); }
  70% { box-shadow: 0 0 0 10px rgba(245, 158, 11, 0); }
  100% { box-shadow: 0 0 0 0 rgba(245, 158, 11, 0); }
}

.ff-badge-text {
  color: var(--gold);
  font-size: 0.8rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
}

.ff-main-title {
  font-size: clamp(3rem, 8vw, 6rem);
  text-transform: uppercase;
  background: linear-gradient(to bottom, #fff, #bbb);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  margin-bottom: var(--space-md);
  text-shadow: 0 10px 30px rgba(0,0,0,0.5);
  position: relative;
  z-index: 1;
}

.ff-subtitle {
  margin: 0 auto var(--space-xl);
  font-size: 1.2rem;
  color: var(--text-secondary);
  position: relative;
  z-index: 1;
}

.btn-gold {
  background: linear-gradient(135deg, var(--gold), #d97706);
  color: #fff;
  border: none;
  font-family: var(--font-display);
  font-size: 1.2rem;
  letter-spacing: 0.05em;
  padding: 16px 32px;
  border-radius: var(--radius);
  position: relative;
  z-index: 1;
  box-shadow: 0 4px 15px rgba(245, 158, 11, 0.4);
}
.btn-gold:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(245, 158, 11, 0.6);
}

.ff-page {
  padding: var(--space-xl) 0 var(--space-3xl);
  display: flex;
  flex-direction: column;
  gap: var(--space-3xl);
}

.ff-section-header {
  margin-bottom: var(--space-xl);
  text-align: center;
}
.ff-section-title {
  font-size: 2.5rem;
  margin-bottom: var(--space-sm);
  color: var(--text-primary);
}

/* -------------------------------------
   QUALIFIED TEAMS (FIXED GRID)
------------------------------------- */
.ff-teams-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--space-xl);
}

.ff-team-card {
  position: relative;
  display: block;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  overflow: hidden;
  transition: all var(--transition-slow);
  text-decoration: none;
}

.ff-team-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
  border-color: rgba(255,255,255,0.2);
}

/* Rank-based styling */
.ff-team-card.rank-1 { border-top: 3px solid var(--gold); }
.ff-team-card.rank-2 { border-top: 3px solid #e2e8f0; } /* Silver */
.ff-team-card.rank-3 { border-top: 3px solid #cd7f32; } /* Bronze */
.ff-team-card.rank-4 { border-top: 3px solid var(--green); }

.ff-team-rank {
  position: absolute;
  top: 16px;
  right: 16px;
  font-family: var(--font-display);
  font-size: 3rem;
  color: rgba(255,255,255,0.05);
  line-height: 1;
  pointer-events: none;
}

.ff-team-body {
  padding: var(--space-xl);
  display: flex;
  flex-direction: column;
  gap: var(--space-lg);
}

.ff-team-header {
  display: flex;
  align-items: center;
  gap: var(--space-md);
  position: relative;
  z-index: 2;
}

.ff-logo-container {
  width: 80px;
  height: 80px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.ff-team-logo {
  width: 100%;
  height: 100%;
  object-fit: contain;
  filter: drop-shadow(0 4px 10px rgba(0,0,0,0.3));
}

.ff-team-logo-fallback {
  display: none;
  font-size: 3rem;
}

.ff-team-info {
  flex: 1;
}

.ff-team-name {
  font-size: 1.8rem;
  margin: 0 0 4px 0;
  color: var(--text-primary);
  line-height: 1.1;
}

.ff-manager-tag {
  display: flex;
  align-items: center;
  gap: 12px;
  font-size: 1rem;
  font-weight: 600;
  color: var(--text-secondary);
  margin-top: 6px;
}

.ff-manager-avatar {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  object-fit: cover;
  border: 2px solid var(--border);
}

.ff-points-box {
  text-align: right;
}
.ff-points-val {
  display: block;
  font-family: var(--font-display);
  font-size: 2.5rem;
  color: var(--text-primary);
  line-height: 1;
}
.ff-points-lbl {
  font-size: 0.7rem;
  text-transform: uppercase;
  color: var(--text-muted);
  letter-spacing: 0.1em;
}

.ff-stats-row {
  display: flex;
  gap: 8px;
  background: var(--bg-elevated);
  padding: 12px;
  border-radius: var(--radius);
  justify-content: space-between;
}

.ff-stat-pill {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.ff-stat-pill .val {
  font-family: var(--font-display);
  font-size: 1.2rem;
  color: var(--text-primary);
  line-height: 1;
}

.ff-stat-pill .lbl {
  font-size: 0.65rem;
  text-transform: uppercase;
  color: var(--text-muted);
  letter-spacing: 0.05em;
  margin-top: 2px;
}

.ff-scorers-box {
  border-top: 1px solid var(--border-subtle);
  padding-top: var(--space-md);
}

.ff-scorers-title {
  font-size: 0.7rem;
  text-transform: uppercase;
  color: var(--text-muted);
  letter-spacing: 0.1em;
  margin-bottom: var(--space-sm);
}

.ff-scorers-list {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.ff-scorer-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 0.9rem;
  color: var(--text-secondary);
}

.ff-scorer-item .goals {
  font-weight: 700;
  color: var(--green);
}

/* -------------------------------------
   SEMI-FINAL MATCHUPS
------------------------------------- */
.ff-matchups-container {
  display: flex;
  flex-direction: column;
  gap: var(--space-2xl);
}

.ff-matchup-card {
  background: linear-gradient(145deg, var(--bg-card), var(--bg-secondary));
  border: 1px solid var(--border);
  overflow: hidden;
  position: relative;
}

.ff-matchup-badge {
  position: absolute;
  top: 0; left: 50%;
  transform: translateX(-50%);
  background: var(--purple);
  color: #fff;
  padding: 4px 16px;
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  border-bottom-left-radius: var(--radius);
  border-bottom-right-radius: var(--radius);
  z-index: 10;
}

.ff-matchup-battle {
  display: flex;
  align-items: center;
  padding: var(--space-2xl) var(--space-xl);
  position: relative;
}

.ff-battle-team {
  flex: 1;
  display: flex;
  align-items: center;
  gap: var(--space-lg);
  text-decoration: none;
  transition: transform var(--transition);
}

.ff-battle-team.home {
  justify-content: flex-end;
  text-align: right;
}

.ff-battle-team.away {
  justify-content: flex-start;
  text-align: left;
}

.ff-battle-team:hover {
  transform: scale(1.02);
}

.ff-battle-logo-wrap {
  width: 120px;
  height: 120px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.ff-battle-logo {
  width: 100%;
  height: 100%;
  object-fit: contain;
  filter: drop-shadow(0 10px 20px rgba(0,0,0,0.4));
}
.ff-battle-fallback { display: none; font-size: 4rem; }

.ff-battle-info {
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.ff-seed {
  font-size: 0.75rem;
  font-weight: 700;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.1em;
  margin-bottom: 4px;
}

.ff-battle-name {
  font-family: var(--font-display);
  font-size: 2.5rem;
  color: var(--text-primary);
  margin: 0;
  line-height: 1;
}

.ff-battle-manager {
  font-size: 0.9rem;
  color: var(--text-secondary);
  margin-top: 4px;
}

.ff-battle-vs {
  padding: 0 var(--space-xl);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  position: relative;
}
.ff-battle-vs::before {
  content: '';
  position: absolute;
  width: 1px;
  height: 150px;
  background: linear-gradient(to bottom, transparent, var(--border), transparent);
}
.ff-battle-vs span {
  font-family: var(--font-display);
  font-size: 1.8rem;
  color: var(--gold);
  background: var(--bg-card);
  padding: 10px 0;
  z-index: 1;
}

.ff-h2h-section {
  background: var(--bg-elevated);
  padding: var(--space-lg) var(--space-xl);
  border-top: 1px solid var(--border);
}

.ff-h2h-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-md);
}

.ff-h2h-header h4 {
  margin: 0;
  font-size: 0.85rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-muted);
}

.ff-h2h-link {
  font-size: 0.8rem;
  color: var(--purple-light);
  font-weight: 600;
}
.ff-h2h-link:hover { color: var(--purple); }

.ff-h2h-table {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.ff-h2h-row {
  display: flex;
  align-items: center;
  padding: 12px 16px;
  background: rgba(0,0,0,0.2);
  border-radius: var(--radius-sm);
  transition: background var(--transition);
  text-decoration: none;
}

.ff-h2h-row:hover {
  background: rgba(0,0,0,0.4);
}

.ff-h2h-row .gw {
  font-size: 0.75rem;
  font-weight: 700;
  color: var(--text-muted);
  width: 50px;
}

.ff-h2h-row .match {
  flex: 1;
  font-size: 0.95rem;
  color: var(--text-primary);
}

.ff-h2h-row .match strong {
  color: var(--gold);
  margin: 0 6px;
}

.ff-h2h-row .result {
  font-size: 0.8rem;
  color: var(--text-secondary);
  text-align: right;
}

.ff-h2h-empty {
  font-size: 0.9rem;
  color: var(--text-muted);
  padding: var(--space-sm) 0;
}

/* -------------------------------------
   TOURNAMENT FORMAT
------------------------------------- */
.ff-bracket-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-lg);
  margin-top: var(--space-xl);
}

.ff-bracket-item {
  background: var(--bg-elevated);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: var(--space-xl);
  text-align: center;
  position: relative;
}

.ff-bracket-item .step {
  width: 32px; height: 32px;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 700;
  font-size: 0.8rem;
  position: absolute;
  top: -16px; left: 50%;
  transform: translateX(-50%);
  color: var(--text-muted);
}

.ff-bracket-item h4 {
  font-size: 1.2rem;
  color: var(--text-primary);
  margin-bottom: 8px;
}

.ff-bracket-item p {
  font-size: 0.9rem;
  margin: 0;
}

.ff-bracket-item.final {
  background: rgba(245, 158, 11, 0.05);
  border-color: rgba(245, 158, 11, 0.3);
}

.ff-bracket-item.final .step {
  background: var(--gold);
  color: #000;
  border-color: var(--gold);
}

.ff-bracket-item.final h4 {
  color: var(--gold);
}

/* -------------------------------------
   PAST WINNERS
------------------------------------- */
.ff-past-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-lg);
}

.ff-past-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--space-xl) var(--space-lg);
  text-align: center;
  transition: transform var(--transition);
}

.ff-past-card:hover { transform: translateY(-4px); }

.ff-past-card .season-badge {
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--text-muted);
  font-weight: 700;
  margin-bottom: var(--space-md);
}

.ff-past-card .trophy-icon {
  font-size: 3rem;
  margin-bottom: var(--space-md);
}

.ff-past-card .champion {
  font-family: var(--font-display);
  font-size: 2rem;
  margin-bottom: var(--space-sm);
  color: var(--text-primary);
}

.ff-past-card .details {
  font-size: 0.85rem;
  margin: 0;
  line-height: 1.5;
}

.ff-past-card.current {
  background: linear-gradient(180deg, rgba(245, 158, 11, 0.08) 0%, var(--bg-card) 100%);
  border-color: rgba(245, 158, 11, 0.4);
}

.gold-text { color: var(--gold) !important; }
.glow { filter: drop-shadow(0 0 10px rgba(245, 158, 11, 0.5)); }

/* -------------------------------------
   WATCH
------------------------------------- */
.ff-watch-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--space-md);
}

.ff-watch-item {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: var(--space-lg);
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  text-decoration: none;
  transition: all var(--transition);
}

.ff-watch-item:hover {
  background: var(--bg-elevated);
  transform: translateY(-2px);
}

.ff-watch-item .icon {
  font-size: 2rem;
  margin-bottom: 8px;
}

.ff-watch-item strong {
  color: var(--text-primary);
  font-size: 1.1rem;
  margin-bottom: 4px;
}

.ff-watch-item span:last-child {
  font-size: 0.85rem;
  color: var(--text-muted);
}

.ff-watch-item.youtube:hover { border-color: #ff0000; }
.ff-watch-item.twitch:hover { border-color: #9146ff; }
.ff-watch-item.sky:hover { border-color: #0072c9; }
.ff-watch-item.live:hover { border-color: var(--gold); }

/* -------------------------------------
   MATCH REPORT LINK (Semi-Finals)
------------------------------------- */
.ff-report-link {
  display: block;
  text-align: center;
  padding: 14px var(--space-xl);
  background: linear-gradient(135deg, var(--purple), rgba(139, 92, 246, 0.1));
  color: #fff;
  font-weight: 700;
  font-size: 0.95rem;
  letter-spacing: 0.03em;
  text-decoration: none;
  transition: all var(--transition);
  border-top: 1px solid var(--border);
}
.ff-report-link:hover {
  background: var(--purple);
}

/* -------------------------------------
   THE GRAND FINAL (Improved)
------------------------------------- */
.ff-gold-glow {
  text-shadow: 0 0 30px rgba(245, 158, 11, 0.5), 0 0 60px rgba(245, 158, 11, 0.2);
  animation: goldPulse 3s ease-in-out infinite;
}
@keyframes goldPulse {
  0%, 100% { text-shadow: 0 0 30px rgba(245, 158, 11, 0.5), 0 0 60px rgba(245, 158, 11, 0.2); }
  50% { text-shadow: 0 0 40px rgba(245, 158, 11, 0.8), 0 0 80px rgba(245, 158, 11, 0.4); }
}

.ff-final-container {
  max-width: 700px;
  margin: 0 auto;
}

.ff-final-card {
  background: linear-gradient(180deg, rgba(245, 158, 11, 0.12) 0%, var(--bg-card) 40%);
  border: 1px solid rgba(245, 158, 11, 0.3);
  box-shadow: 0 0 40px rgba(245, 158, 11, 0.08);
  text-align: center;
  overflow: hidden;
}

.ff-final-badge {
  position: absolute;
  top: 0; left: 50%;
  transform: translateX(-50%);
  background: linear-gradient(135deg, var(--gold), #d97706);
  color: #000;
  padding: 4px 20px;
  font-size: 0.8rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  border-bottom-left-radius: var(--radius);
  border-bottom-right-radius: var(--radius);
  z-index: 10;
}

.ff-final-battle {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-xl);
  padding: var(--space-3xl) var(--space-xl) var(--space-xl);
}

.ff-final-team {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  position: relative;
}
.ff-final-team.win {
  filter: drop-shadow(0 0 20px rgba(245, 158, 11, 0.3));
}

.ff-final-logo-wrap {
  width: 100px;
  height: 100px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 8px;
}

.ff-final-logo {
  width: 100%;
  height: 100%;
  object-fit: contain;
  filter: drop-shadow(0 8px 20px rgba(0,0,0,0.5));
}
.ff-final-fallback { display: none; font-size: 4rem; }

.ff-final-team.win .ff-final-logo {
  filter: drop-shadow(0 0 20px rgba(245, 158, 11, 0.4));
}

.ff-final-name {
  font-family: var(--font-display);
  font-size: 2rem;
  color: var(--text-primary);
  margin: 0;
  line-height: 1.1;
}
.ff-final-team.win .ff-final-name {
  color: var(--gold);
}

.ff-final-manager {
  font-size: 0.85rem;
  color: var(--text-secondary);
}
.ff-final-seed {
  font-size: 0.7rem;
  font-weight: 700;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.ff-final-score {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 0 var(--space-lg);
}
.ff-final-score-val {
  font-family: var(--font-display);
  font-size: 5rem;
  color: var(--text-primary);
  line-height: 1;
}
.ff-final-score-val.win {
  color: var(--gold);
  text-shadow: 0 0 30px rgba(245, 158, 11, 0.4);
}
.ff-final-score-sep {
  font-size: 3rem;
  color: var(--text-muted);
  font-weight: 300;
}

.ff-final-champion {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-lg);
  padding: var(--space-lg) var(--space-xl) var(--space-xl);
  background: linear-gradient(180deg, transparent 0%, rgba(245, 158, 11, 0.08) 100%);
  border-top: 1px solid rgba(245, 158, 11, 0.15);
}
.ff-trophy-wrap {
  font-size: 4rem;
  line-height: 1;
  animation: trophyBounce 2s ease-in-out infinite;
}
@keyframes trophyBounce {
  0%, 100% { transform: translateY(0) rotate(-3deg); }
  50% { transform: translateY(-8px) rotate(3deg); }
}
.ff-champion-text {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}
.ff-champion-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: var(--text-muted);
  font-weight: 700;
}
.ff-champion-team {
  font-family: var(--font-display);
  font-size: 2.5rem;
  color: var(--gold);
  line-height: 1.1;
  text-shadow: 0 0 20px rgba(245, 158, 11, 0.3);
}

.ff-final-links {
  display: flex;
  gap: var(--space-md);
  padding: 0 var(--space-xl) var(--space-xl);
  justify-content: center;
}
.ff-final-links .btn-ghost {
  color: var(--text-secondary);
  border-color: var(--border);
}
.ff-final-links .btn-ghost:hover {
  color: var(--text-primary);
  border-color: var(--text-muted);
}

/* -------------------------------------
   RESPONSIVE
------------------------------------- */
@media (max-width: 1024px) {
  .ff-matchup-battle {
    flex-direction: column;
    gap: var(--space-xl);
  }
  .ff-battle-team.home { text-align: center; justify-content: center; flex-direction: column-reverse; }
  .ff-battle-team.away { text-align: center; justify-content: center; flex-direction: column; }
  .ff-battle-vs::before { display: none; }
  .ff-battle-vs { padding: var(--space-md) 0; }
  
  .ff-teams-grid { grid-template-columns: 1fr; }
}

@media (max-width: 768px) {
  .ff-bracket-grid, .ff-past-grid { grid-template-columns: 1fr; }
  .ff-watch-grid { grid-template-columns: repeat(2, 1fr); }
  
  .ff-team-header { flex-direction: column; text-align: center; }
  .ff-points-box { text-align: center; margin-top: var(--space-sm); }
  .ff-manager-tag { justify-content: center; }
  
  .ff-stats-row { flex-wrap: wrap; justify-content: center; gap: 16px; }
  
  .ff-h2h-row { flex-direction: column; gap: 8px; text-align: center; }
  .ff-h2h-row .result { text-align: center; }
}
</style>
